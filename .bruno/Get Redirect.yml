info:
  name: Get Redirect
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/:slug
  params:
    - name: slug
      value: wlLUbw
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: false
  maxRedirects: 5
  forwardAuthorizationHeader: true
