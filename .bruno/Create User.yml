info:
  name: Create User
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/sign-up
  body:
    type: json
    data: |-
      {
        "username": "theo",
        "password": "test"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
