info:
  name: Sign In
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/sign-in
  body:
    type: json
    data: |-
      {
        "username": "theo",
        "password": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
