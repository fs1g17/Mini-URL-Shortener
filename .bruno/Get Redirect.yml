info:
  name: Get Redirect
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/:slug
  params:
    - name: slug
      value: a0938d
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
