info:
  name: hello world
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/hello-world
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
