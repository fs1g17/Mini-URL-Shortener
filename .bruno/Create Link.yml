info:
  name: Create Link
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/link
  body:
    type: json
    data: |-
      {
        "longURL": "https://google.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
